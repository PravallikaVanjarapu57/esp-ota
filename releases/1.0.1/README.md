# Firmware Release 1.0.1

This release includes the initial RTOS-based firmware build with:

- WiFi connection to network `TRINITY`
- Periodic OTA checking via Google Drive
- NTP time synchronization
- LED blink interval set to **100ms**
- Full diagnostics output every second
- Uptime counter and version reporting

Source file: `main.cpp` (copied from project root)
