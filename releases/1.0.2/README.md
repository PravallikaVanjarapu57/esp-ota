# Firmware Release 1.0.2

This release is identical to 1.0.1 except for the blink interval and version number:

- Firmware version now **1.0.2**
- LED blink delay changed to **2 seconds** (2000ms)

Source file: `main.cpp` (copied from project root)

## Notes
Keep the `releases` folder updated with full source for each published firmware. Documentation and version history is stored in the respective release README files.